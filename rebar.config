{erl_opts, [debug_info]}.

{deps, [
    {lux, {git, "https://github.com/hawk/lux.git", {branch, "master"}}}
]}.

{post_hooks, [
    %% To build the `runpty' binary that lux is using
    {compile, "echo \"Consider: cd ${PWD}/../lux ; autoconf ; ./configure ; make\""}
]}.
